<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <include layout="@layout/action_bar"/>

    <TextView
        android:background="@drawable/edit_text_main"
        android:id="@+id/order_submission_order_submitted"
        android:text="@string/order_submitted"
        android:textSize="24sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:padding="16dp"
        android:layout_margin="4dp"/>

    <Button
        android:id="@+id/order_submission_back_to_main"
        android:background="@drawable/button_main"
        android:layout_below="@+id/order_submission_order_submitted"
        android:text="@string/home"
        android:textColor="@color/white"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_margin="4dp"/>


</RelativeLayout>